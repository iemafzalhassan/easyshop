apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easyshop.fullname" . }}-test-connection"
  labels:
    {{- include "easyshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "easyshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
